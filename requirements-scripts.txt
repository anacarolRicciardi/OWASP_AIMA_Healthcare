# Dependencies for scripts/generate_pdf_preview.py (documentation PDF build).
# Not required by the deployed Streamlit app — kept separate from requirements.txt
# so the public web app's dependency/build footprint stays minimal.
Markdown>=3.6,<4.0.0
weasyprint>=62.0,<63.0
